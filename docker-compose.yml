version: "3.9"

services:
  backend:
    build: 
      context: backend/
    ports:
      - "8080:8080"
    networks:
      - resolve-net
    # volumes: 
    #   - ./:/app

  gotest:
    build: 
      context: backend/
      dockerfile: Dockerfile.test
    command: ["go","test","./..."]
    volumes:
      - ./:/app

networks:
  resolve-net:
    driver: bridge